# Data

Static public data assets live under this directory.

Only store data here when it is public, cacheable, versioned, and safe to serve directly from the asset CDN. Do not use this directory as a replacement for backend-owned catalog data or user-specific API responses.
